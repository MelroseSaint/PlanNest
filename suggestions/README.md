# AI Suggestions

This directory is designated for AI-generated content (lesson plans, templates, etc.) to ensure separation from the hand-coded UI components.

When using AI Studio or similar tools, target this directory for output. The application services can then ingest content from here (or simulate ingestion via API/Cache in a browser environment).